// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: ring.sql

package pg

import (
	"context"
)

const ensureProjectSeq = `-- name: EnsureProjectSeq :exec
INSERT INTO project_seq (project_id, next)
VALUES ($1, 1)
ON CONFLICT (project_id) DO NOTHING
`

// Create the seq row for a new project if absent (idempotent). Called at
// project creation so LeaseSeqBlock never no-ops on a missing row.
func (q *Queries) EnsureProjectSeq(ctx context.Context, projectID int64) error {
	_, err := q.db.Exec(ctx, ensureProjectSeq, projectID)
	return err
}

const leaseSeqBlock = `-- name: LeaseSeqBlock :one
UPDATE project_seq
   SET next = next + $1
 WHERE project_id = $2
 RETURNING (next - $1)::bigint AS start_seq
`

type LeaseSeqBlockParams struct {
	BlockSize int64
	ProjectID int64
}

// Atomically lease a block of sequence values for a project. The caller owns
// [start_seq, start_seq + block_size); holes from unused tails on restart are
// acceptable — seq is an order marker, not a row counter (plan ring/seq §2.3.1).
func (q *Queries) LeaseSeqBlock(ctx context.Context, arg LeaseSeqBlockParams) (int64, error) {
	row := q.db.QueryRow(ctx, leaseSeqBlock, arg.BlockSize, arg.ProjectID)
	var start_seq int64
	err := row.Scan(&start_seq)
	return start_seq, err
}
