// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: probe/v1/probe.proto

package probev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "go.upcontrol.io/back/gen/rpc/probe/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ProbeServiceName is the fully-qualified name of the ProbeService service.
	ProbeServiceName = "upcontrol.probe.v1.ProbeService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ProbeServiceLeaseProcedure is the fully-qualified name of the ProbeService's Lease RPC.
	ProbeServiceLeaseProcedure = "/upcontrol.probe.v1.ProbeService/Lease"
	// ProbeServiceSubmitResultsProcedure is the fully-qualified name of the ProbeService's
	// SubmitResults RPC.
	ProbeServiceSubmitResultsProcedure = "/upcontrol.probe.v1.ProbeService/SubmitResults"
	// ProbeServiceReportBlindProcedure is the fully-qualified name of the ProbeService's ReportBlind
	// RPC.
	ProbeServiceReportBlindProcedure = "/upcontrol.probe.v1.ProbeService/ReportBlind"
)

// ProbeServiceClient is a client for the upcontrol.probe.v1.ProbeService service.
type ProbeServiceClient interface {
	// Lease: the probe takes a batch of work. The lease has a TTL of two
	// intervals; an unacked lease is re-issued after expiry.
	Lease(context.Context, *connect.Request[v1.LeaseRequest]) (*connect.Response[v1.LeaseResponse], error)
	// SubmitResults: results in a batch. Idempotent per check_id.
	SubmitResults(context.Context, *connect.Request[v1.SubmitResultsRequest]) (*connect.Response[v1.SubmitResultsResponse], error)
	// ReportBlind: the probe declares itself blind — this cycle's results were
	// DISCARDED by the probe itself (e.g. its anchor sites were unreachable, so
	// its measurements cannot be trusted). This is what prevents false incidents.
	ReportBlind(context.Context, *connect.Request[v1.ReportBlindRequest]) (*connect.Response[v1.ReportBlindResponse], error)
}

// NewProbeServiceClient constructs a client for the upcontrol.probe.v1.ProbeService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewProbeServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ProbeServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	probeServiceMethods := v1.File_probe_v1_probe_proto.Services().ByName("ProbeService").Methods()
	return &probeServiceClient{
		lease: connect.NewClient[v1.LeaseRequest, v1.LeaseResponse](
			httpClient,
			baseURL+ProbeServiceLeaseProcedure,
			connect.WithSchema(probeServiceMethods.ByName("Lease")),
			connect.WithClientOptions(opts...),
		),
		submitResults: connect.NewClient[v1.SubmitResultsRequest, v1.SubmitResultsResponse](
			httpClient,
			baseURL+ProbeServiceSubmitResultsProcedure,
			connect.WithSchema(probeServiceMethods.ByName("SubmitResults")),
			connect.WithClientOptions(opts...),
		),
		reportBlind: connect.NewClient[v1.ReportBlindRequest, v1.ReportBlindResponse](
			httpClient,
			baseURL+ProbeServiceReportBlindProcedure,
			connect.WithSchema(probeServiceMethods.ByName("ReportBlind")),
			connect.WithClientOptions(opts...),
		),
	}
}

// probeServiceClient implements ProbeServiceClient.
type probeServiceClient struct {
	lease         *connect.Client[v1.LeaseRequest, v1.LeaseResponse]
	submitResults *connect.Client[v1.SubmitResultsRequest, v1.SubmitResultsResponse]
	reportBlind   *connect.Client[v1.ReportBlindRequest, v1.ReportBlindResponse]
}

// Lease calls upcontrol.probe.v1.ProbeService.Lease.
func (c *probeServiceClient) Lease(ctx context.Context, req *connect.Request[v1.LeaseRequest]) (*connect.Response[v1.LeaseResponse], error) {
	return c.lease.CallUnary(ctx, req)
}

// SubmitResults calls upcontrol.probe.v1.ProbeService.SubmitResults.
func (c *probeServiceClient) SubmitResults(ctx context.Context, req *connect.Request[v1.SubmitResultsRequest]) (*connect.Response[v1.SubmitResultsResponse], error) {
	return c.submitResults.CallUnary(ctx, req)
}

// ReportBlind calls upcontrol.probe.v1.ProbeService.ReportBlind.
func (c *probeServiceClient) ReportBlind(ctx context.Context, req *connect.Request[v1.ReportBlindRequest]) (*connect.Response[v1.ReportBlindResponse], error) {
	return c.reportBlind.CallUnary(ctx, req)
}

// ProbeServiceHandler is an implementation of the upcontrol.probe.v1.ProbeService service.
type ProbeServiceHandler interface {
	// Lease: the probe takes a batch of work. The lease has a TTL of two
	// intervals; an unacked lease is re-issued after expiry.
	Lease(context.Context, *connect.Request[v1.LeaseRequest]) (*connect.Response[v1.LeaseResponse], error)
	// SubmitResults: results in a batch. Idempotent per check_id.
	SubmitResults(context.Context, *connect.Request[v1.SubmitResultsRequest]) (*connect.Response[v1.SubmitResultsResponse], error)
	// ReportBlind: the probe declares itself blind — this cycle's results were
	// DISCARDED by the probe itself (e.g. its anchor sites were unreachable, so
	// its measurements cannot be trusted). This is what prevents false incidents.
	ReportBlind(context.Context, *connect.Request[v1.ReportBlindRequest]) (*connect.Response[v1.ReportBlindResponse], error)
}

// NewProbeServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewProbeServiceHandler(svc ProbeServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	probeServiceMethods := v1.File_probe_v1_probe_proto.Services().ByName("ProbeService").Methods()
	probeServiceLeaseHandler := connect.NewUnaryHandler(
		ProbeServiceLeaseProcedure,
		svc.Lease,
		connect.WithSchema(probeServiceMethods.ByName("Lease")),
		connect.WithHandlerOptions(opts...),
	)
	probeServiceSubmitResultsHandler := connect.NewUnaryHandler(
		ProbeServiceSubmitResultsProcedure,
		svc.SubmitResults,
		connect.WithSchema(probeServiceMethods.ByName("SubmitResults")),
		connect.WithHandlerOptions(opts...),
	)
	probeServiceReportBlindHandler := connect.NewUnaryHandler(
		ProbeServiceReportBlindProcedure,
		svc.ReportBlind,
		connect.WithSchema(probeServiceMethods.ByName("ReportBlind")),
		connect.WithHandlerOptions(opts...),
	)
	return "/upcontrol.probe.v1.ProbeService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ProbeServiceLeaseProcedure:
			probeServiceLeaseHandler.ServeHTTP(w, r)
		case ProbeServiceSubmitResultsProcedure:
			probeServiceSubmitResultsHandler.ServeHTTP(w, r)
		case ProbeServiceReportBlindProcedure:
			probeServiceReportBlindHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedProbeServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedProbeServiceHandler struct{}

func (UnimplementedProbeServiceHandler) Lease(context.Context, *connect.Request[v1.LeaseRequest]) (*connect.Response[v1.LeaseResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("upcontrol.probe.v1.ProbeService.Lease is not implemented"))
}

func (UnimplementedProbeServiceHandler) SubmitResults(context.Context, *connect.Request[v1.SubmitResultsRequest]) (*connect.Response[v1.SubmitResultsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("upcontrol.probe.v1.ProbeService.SubmitResults is not implemented"))
}

func (UnimplementedProbeServiceHandler) ReportBlind(context.Context, *connect.Request[v1.ReportBlindRequest]) (*connect.Response[v1.ReportBlindResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("upcontrol.probe.v1.ProbeService.ReportBlind is not implemented"))
}
